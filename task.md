# TODO

1. Rename the generic `CD бр.` result.
   - For D111/D116 the label is not accurate as a material name.
   - Consider `Профили общо (бр.)` or system-specific column labels.

2. Split material takeoff by construction type.
   - D113 currently has a reasonable geometric check for rows/connectors/hangers.
   - D111/D112/D116 should not all use the same `CD`, `UD`, `Връзки`, `Окачвачи` material model.
   - Add construction-specific material rows if the app is intended to produce a purchase list.

3. Improve `Дюбели UD` handling.
   - Current formula uses a single UD fixing spacing field.
   - Knauf distinguishes cases such as load-bearing/non-load-bearing connection and fire protection.
   - Add a dropdown or automatic rule for 625/1000 mm instead of relying on the user to know the value.

4. Show auto/manual state for `a`, `b`, `c`.
   - Values are auto-filled from Knauf tables, but can be manually changed.
   - Add an indicator such as `Автоматично по Knauf` / `Ръчно променено`.
   - Add a `Върни по Knauf` action.

5. Add more unit tests for Knauf table values.
   - Current test covers the known D113 10 x 10 m example.
   - Add D113 table tests, for example `c = 600`:
     - `0.15 -> a = 1150`
     - `0.30 -> a = 900`
     - `0.40 -> a = 800`
     - `0.50 -> a = 750`
     - `0.65 -> a = 700`

6. Improve drawing positions for installation use.
   - Current visualization keeps profiles/hangers inside the room and distributes them between offsets.
   - For an installation-grade drawing, positions should be generated by maximum allowed spacing and the final profile should remain inside the room without stretching all positions evenly.
